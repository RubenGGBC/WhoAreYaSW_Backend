describe('Operaciones matemáticas básicas', () => {
    test('suma 1 + 2 debe ser 3', () => {
        expect(1 + 2).toBe(3);
    });

    test('multiplicación 3 * 4 debe ser 12', () => {
        expect(3 * 4).toBe(12);
    });
});